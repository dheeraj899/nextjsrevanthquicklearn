{
  "compilerOptions": {
    "baseUrl": "app",  // Set the base URL to the "app" folder
    "paths": {
      "@/*": ["./*"]    // Map "@" to the root of the "app" folder
    }
  },
  "include": ["app/**/*"]  // Include everything inside the "app" folder
}
